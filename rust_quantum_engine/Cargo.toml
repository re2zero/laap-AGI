[package]
name = "quantum_engine"
version = "0.1.0"
edition = "2021"
description = "LAAP Quantum Engine - Rust performance layer for quantum cognition models"

[dependencies]
complex = "0.4"
pyo3 = { version = "0.21", features = ["extension-module"] }

[lib]
name = "quantum_engine"
crate-type = ["cdylib", "rlib"]